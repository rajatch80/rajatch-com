{
  "name": "portfolio_website",
  "version": "0.1.3",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start -p 8080",
    "gcp-build": "npm run build",
    "deploy": "gcloud config set project porfolio-react && gcloud app deploy app.yaml --version=0-1-3"
  },
  "dependencies": {
    "next": "10.2.3",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "styled-components": "^5.3.0",
    "styled-normalize": "^8.0.7",
    "react-icons": "^4.2.0"
  }
}
